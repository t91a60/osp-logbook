SECRET_KEY=twoja_super_tajna_wartosc_produkcyjna
DATABASE_URL=postgresql://user:password@localhost:5432/osp_logbook
OSP_USE_HTTPS=0
FLASK_ENV=development

# --- Lokalny Development ---
# python run.py (odpala serwer Flask z auto-reload)

# --- Render Deployment Notes ---
# Render rozpoznaje aplikacje klasy gunicorn z pliku app:app.
# Start Command: gunicorn app:app
# Ewentualnie Pre-Deploy: python -m yoyo apply -b --database $DATABASE_URL ./migrations
