services:
  - type: web
    name: osp-logbook
    env: docker
    region: frankfurt
    plan: free
    preDeployCommand: yoyo apply -b --database $DATABASE_URL ./migrations
    healthCheckPath: /health
    envVars:
      - key: FLASK_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: osp-logbook-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true

databases:
  - name: osp-logbook-db
    databaseName: osp_logbook
    user: logbook_user
    plan: free
